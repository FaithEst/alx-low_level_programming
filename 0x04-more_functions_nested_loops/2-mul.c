#include "main.h"

/**
 * mul -  multiplies two integers.
 * @a: the 1st digit to be checked.
 * @b: the 2nd digit to be checked.
 * Return: product
 */
int mul(int a, int b)
{
int product = a * b;

return (product);
}
